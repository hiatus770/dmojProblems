#include <bits/stdc++.h>
using namespace std;
const int MD = 1001; 
#define inputJunk ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);
#define max 1501

int n, m, k; 

int grid[max+1][max+1]; 


int main(){
    inputJunk 
    cin >> n >> m >> k; 
    int z = 0; 
    for (int i = 0; i < n; i++){
        for (int j = 0; j < m; j++){
            cin >> grid[j][i]; 
            if (grid[j][i] != 0){
                
            } 
        }
    }


}