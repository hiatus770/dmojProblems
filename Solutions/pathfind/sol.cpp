#include <bits/stdc++.h>
using namespace std;
#define inputJunk ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);
#define ell "\n";
#define f first
#define s second
#define pb push_back
#define forl(n) for (int i = 0; i < n; i++)
typedef long long ll; 
typedef pair<int, int> pInt; 
int n, m, k; 

/*

Strategy for solving problem 

- Find out if two walls are touching via the blocked points on the map
- Side left down to side up right nothing else works to block it 
- Start a bfs from each group and see how much borders each group touches in the rectangle 
 */ 

int main(){
    cin >> n >> m >> k; 
    forl(k){
    
    }
}